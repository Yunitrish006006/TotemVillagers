{
  "schemaVersion": 1,
  "id": "totem-villagers",
  "version": "${version}",
  "name": "Totem Villagers",
  "icon": "assets/totem-villagers/icon.png",
  "description": "Server-authoritative work-backed villager trading for the Totem platform.",
  "environment": "*",
  "entrypoints": {
    "main": ["dev.totem.villagers.TotemVillagers"],
    "client": ["dev.totem.villagers.client.TotemVillagersClient"]
  },
  "mixins": [
    "totem-villagers.mixins.json",
    {
      "config": "totem-villagers.client.mixins.json",
      "environment": "client"
    }
  ],
  "depends": {
    "fabricloader": ">=0.19.3",
    "fabric-api": "*",
    "minecraft": "~26.2",
    "java": ">=25",
    "totem-core": ">=0.7.0 <0.8.0"
  },
  "suggests": {
    "totem-remnant": ">=0.2.13"
  }
}
