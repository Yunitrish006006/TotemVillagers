# Local manual visual review

Run the module's visual/client tests first, then inspect the generated images
locally with:

    bash scripts/manual-visual-review.sh

The GitHub workflow remains responsible for objective automated tests and
artifact generation. Subjective visual approval is local and must be completed
before publishing.
